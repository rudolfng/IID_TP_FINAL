name: 'astronomy_observatory'
version: '1.0.0'
config-version: 2

profile: 'astronomy_observatory'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  astronomy_observatory:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
